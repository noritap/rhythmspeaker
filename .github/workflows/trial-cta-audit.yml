name: Trial CTA Audit

on:
  pull_request:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Compile trial CTA audit
        run: python -m py_compile tools/trial_cta_audit.py
      - name: Enforce trial LINE conversion contract
        run: python tools/trial_cta_audit.py --strict

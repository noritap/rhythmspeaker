# `g` Decision — Ecosystem UX

Date: 2026-09-18

Highest-value safe next action was changed from speculative visual redesign to evidence-backed semantic UX cleanup.

Reason:

- Current main already contains ECOSYSTEM UI v2.
- Public 7-category IA is established and should be preserved.
- Source inspection found concrete semantic inconsistencies that can be fixed with low blast radius.
- Rendered browser verification is unavailable in the current execution environment, so visual claims are deferred rather than guessed.

Decision:

`KEEP UI v2 -> FIX PUBLIC SEMANTICS -> VERIFY -> RELEASE`
